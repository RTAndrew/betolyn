meta {
  name: matches raw sql
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/debug/perf/matches/sql
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  Same nested JSON as GET /matches, fetched with one plain SQL query (no JPA).
  Compare data.perf.queryMs against GET /matches latency.
}
