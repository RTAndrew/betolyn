meta {
  name: create AUTO event
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/spaces/{{spaceId}}/matches
  body: json
  auth: inherit
}

body:json {
  {
    "matchId": "{{matchId}}",
    "maxReservedLiability": 1000
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
