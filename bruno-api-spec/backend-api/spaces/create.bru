meta {
  name: create
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/spaces
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Weekend League",
    "description": "Private betting pool for friends.",
    "userIds": []
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
